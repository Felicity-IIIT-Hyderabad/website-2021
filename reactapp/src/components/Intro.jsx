import { Container } from "reactstrap";

const Intro = () => {
    return (
        <Container fluid className="full-page text-light">
            intro
        </Container>
    );
};

export default Intro;
